		<script>
		// Material Select Initialization
		$(document).ready(function() {
		$('.mdb-select').materialSelect();
		});
		</script>
--> 


<tbody  id="cargar">
          <tr>
            <td>1</td>
            <td>Mostaza</td>
            <td><button class="btn btn-primary">
                <i class="material-icons">edit</i>
              </button> </td>
            <td><button class="btn btn-primary">
                <i class="material-icons">clear</i>
              </button></td>
          </tr>
          <tr>
            <td>2</td>
            <td>Mantequilla</td>
            <td><button class="btn btn-primary">
                <i class="material-icons">edit</i>
              </button></td>
            <td><button class="btn btn-primary">
                <i class="material-icons">clear</i>
              </button></td>
          </tr>
          <tr>
            <td>3</td>
            <td>Frijol</td>
            <td><button class="btn btn-primary">
                <i class="material-icons">edit</i>
              </button></td>
            <td><button class="btn btn-primary">
                <i class="material-icons">clear</i>
              </button></td>
          </tr>
        </tbody>

//ya inserta
        function obtenerIngrediente(){
//  var ingredientes=new Array();
  var ingrediente= document.getElementById("ingrediente1").value
  var numero=0;
  //console.log(ingrediente)
  if(ingrediente.length==0){
    alert("El campo esta vacio");
  }else{
  
   // ingredientes[j]=ingrediente;
   var arrayDato=arrayJson(numero,ingrediente);
   console.log(arrayDato);
   var inserta= firebase.database().ref("receta/+id");
  inserta.set(arrayDato);
  alert("se insertor");

  // numero==arrayDato.length;
   //numero++;
  }
 /* for(i=1;i<arrayDato.length;i++){
    console.log("Estos son los ingedientes  :" + i + arrayDato[i] )
  }
*/
}




//este era el intento de hacerlo local


function obtenerIngrediente(){
  var ingrediente= document.getElementById("ingrediente1").value
   if(ingrediente.length==0){
  alert("El campo esta vacio");
  }else{
  
  //ingredientes[j]=ingrediente;
//   var arrayDato=arrayJson(numero,ingrediente);
  // console.log(arrayDato);
  // var inserta= firebase.database().ref("receta/+id");
  //inserta.set(arrayDato);
  //alert("se insertor");
 arrelo(ingrediente)
  // numero==arrayDato.length;
   //numero++;
   //console.log(ingredientes.length)
  }
  //for(i=1;i<ingredientes.length;i++){
   // console.log("Estos son los ingedientes  :" + i + ingredientes[i] )
 // }

}
function arrelo(ingrediente){
  var ingredientes=new Array();
  ingredientes.push(ingrediente);
  console.log(ingredientes);
  console.log(ingredientes.length);

  for(i=1;i<ingredientes.length;i++){
  console.log("Estos son los ingedientes  :" + i + ingredientes[i] )
  }
}
function updateIngredientes(ii, nombre,key){
  db.ref('ingredientes/'+key).update({
        name:name,
        description:description,
    });
}

/*
function getId(){
  return document.getElementById(id).value
}
function myFunction(){
  var ingrediente =getId("ingrediente1");
  console.log(receta);
  alert("Agregaste un elemento en la pagina web" + ingrediente);
}
*/
/*
$(function(){
var app = firebase.database();
var appReferencia=app.ref("recetas/");

});